interface:
  display_name: "B2C Mobile Launch"
  short_description: "Run B2C app launch autopilot"
  brand_color: "#FF6F5C"
  default_prompt: "Take my B2C mobile app idea, transcript, spec, or repo and run the launch autopilot until a real founder-only gate. Create PROJECT_STATE.yaml, launch-cockpit.html, ORCHESTRATION.md, launch artifacts, SECURITY.md, source freshness proof, validators, and production-readiness proof."

policy:
  allow_implicit_invocation: true
