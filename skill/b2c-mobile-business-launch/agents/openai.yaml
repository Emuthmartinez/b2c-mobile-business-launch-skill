interface:
  display_name: "B2C Mobile Launch"
  short_description: "Launch, submit, and market B2C apps"
  brand_color: "#FF6F5C"
  default_prompt: "Use $b2c-mobile-business-launch to create a launch-ready B2C app package with Doppler secrets, attribution, store ops, production proof, and app-local agents."

policy:
  allow_implicit_invocation: true
