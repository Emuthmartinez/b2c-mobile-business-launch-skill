# Failure Cards

Use failure cards when a blocker or recurring launch risk needs to survive the current conversation. They are small, explicit, and testable.

## Card Shape

```yaml
id: "posthog-person-property-missing"
severity: "high"
owner: "engineering-leader"
status: "open"
detected_at: "YYYY-MM-DD"
evidence:
  - "ANALYTICS.md"
impact: "Attribution cannot be used in cohorts, dashboards, or lifecycle messaging."
next_action: "Persist self_reported_source to PostHog person properties and backend profile, then verify."
validator: "npm run check:attribution -- --root ."
```

Store active cards in `PROJECT_STATE.yaml` and optionally mirror longer cards in `FAILURE_CARDS.md` when the launch is large.

## Provider Failure Cards

Use these as defaults and specialize them to the current app.

| ID | Trigger | Required Fix |
| --- | --- | --- |
| `doppler-config-name-and-credential-routing` | (a) `doppler run --config` uses a name not confirmed from `SECRETS.md` or `doppler configs`; (b) `source` used on a non-POSIX env or credential file; (c) JavaScript template literals nested inside a double-quoted bash heredoc passed to `doppler run -- bash -lc`; (d) committed markdown contains awk/grep extraction snippet that prints a raw credential | (a) Read `SECRETS.md` config map or run `doppler configs --project <name>` before any `doppler run` command — never guess; (b) replace `source` with `awk -F= '/^KEY=/{print $2}'` for single-value extraction; (c) use `doppler run -- node --input-type=module <<'NODE' ... NODE` (single-quoted heredoc) for JS that contains template literals; (d) remove extraction snippets from committed docs and replace with `doppler run --` command references; run `npm run check:secrets -- --root .` |
| `doppler-docs-stale` | Doppler setup or service-token instructions rely on memory | Refresh official docs and local CLI help, then update `SECRETS.md` with docs/version basis |
| `secret-routing-missing` | new env var/key/webhook secret appears without `SECRETS.md` update | classify public/server-only, provider location, command wrapper, CI injection, and rotation note |
| `security-release-lane-missing` | launch-ready claim exists without `SECURITY.md`, threat model, scanner/review route, platform hardening, incident response, and accepted-risk proof | create `SECURITY.md`, render `security-review.html`, route paid/free security tools, run `check:security`, attach scanner/review proof or founder-approved blocked route |
| `security-tool-fallback-unapproved` | Claude Security, Codex Security, GitHub Advanced Security, Snyk/Semgrep/Socket, MobSF Cloud, or commercial app-integrity route is replaced by a free route without approval | stop, record route in `TOOL_DECISIONS.md` and `PROJECT_STATE.yaml`, ask for access/approval, then run the approved route |
| `posthog-person-property-missing` | attribution only emits event | set person property, backend/profile persistence, anonymous-to-identified reconciliation, and proof |
| `revenuecat-entitlement-unproven` | products/offering exist but app access is not validated | sandbox purchase/restore proves entitlement in app, RevenueCat, and backend/profile |
| `app-price-vs-iap-confusion` | store pricing work begins and no explicit confirmation exists that the app container price is Free and any Lifetime offer uses a NON_CONSUMABLE IAP SKU | surface the distinction before pricing work; verify app container price is Free; verify a NON_CONSUMABLE IAP product exists for each Lifetime offer in `REVENUE_OPS.md`/`STORE_CONSOLE.md`; run `npm run check:store-console -- --root .` |
| `stripe-webhook-unproven` | Stripe checkout exists without webhook/idempotency proof | verify webhook signature, idempotency, entitlement projection, and failure recovery |
| `resend-template-unbranded` | email templates ignore `DESIGN.md` or legal footer | map brand tokens, sender, support reply-to, unsubscribe, idempotency, and provider log proof |
| `sentry-unverified` | SDK configured but no non-production event or alert proof | send test event, verify release/environment tags, PII scrubbing, and alert routing |
| `asc-name-fallback-unapproved` | ASC CLI proposes fallback app name | stop, record collision, ask founder, update brand/store/screenshots/revenue docs if approved |
| `asc-command-guidance-stale` | upstream ASC CLI or skill pack changes but references keep stale command families | refresh Rork skill pack, local `asc --help`, official Apple docs, then update references/templates/evals |
| `store-screenshots-raw-only` | raw app captures are treated as final App Store screenshots | create `SCREENSHOTS.md`, compose iPhone/iPad/App Store assets with copy overlays and design-system frames, validate dimensions/alpha/color space, attach visual QA, and keep upload founder-approved |
| `apple-privacy-manifest-unproven` | iOS App Store upload readiness is claimed without `APPLE_APP_STORE_REQUIREMENTS.md`, bundled `PrivacyInfo.xcprivacy`, required reason API audit, SDK manifest/signature inventory, Xcode privacy report, purpose strings, ATT decision, account deletion, review notes, and upload-warning status | create the packet, reconcile official Apple docs with code/SDKs/App Privacy answers, add/verify `PrivacyInfo.xcprivacy`, run `npm run check:apple-requirements -- --root .`, and block ASC upload until resolved |
| `source-registry-url-untracked` | new external docs/tool URL appears without `source-registry.yaml` entry | run `check:source-freshness -- --write-discovered`, review candidate source, and commit registry update |
| `weekly-source-refresh-unreviewed` | weekly bot treats discovered or changed sources as accepted policy without review | keep PR blocked until capability analysis, reference/template updates, and LaunchBench pass |
| `eleven-star-experience-missing` | product/spec/design/build work starts without `11_STAR_EXPERIENCE.md`, visual ladder, line of feasibility, and V1 scalable slice | create the experience contract, render `11-star-experience.html`, update `SPEC.md`, `LAUNCH_TRACE.md`, `DESIGN.md`, `ONBOARDING.md`, and `TECH_SPEC.md`, then run `npm run check:11-star -- --root .` |
| `paid-user-acquisition-system-missing` | paid growth is marked ready without `PAID_UA.md`, one-channel focus, creative cadence, baseline/blended report, RevenueCat LTV/CPA review, stop/scale rules, and founder spend approval | create `PAID_UA.md`, select or defer one channel, define creative production, baseline and blended reporting, RevenueCat economics, store destination, founder gates, then run `npm run check:paid-ua -- --root .` |
| `viral-growth-loop-missing` | social growth is marked ready from UGC ideas, views, or TikTok hooks without a product-led loop contract | create `VIRAL_GROWTH.md`, define referral/share mechanics, abuse controls, format lab, monetization timing, analytics proof, and stop/scale rules, then run `npm run check:viral-growth -- --root .` |
| `orchestration-preflight-missing` | broad launch, multi-lane build, or subagent-assisted work starts without `ORCHESTRATION.md` and top-level `PROJECT_STATE.yaml` orchestration state | write the preflight, critical-path split, candidate units, strategy, serialized resources, and validator plan before dispatch or readiness claims |
| `parallel-file-overlap-unsafe` | two parallel units edit the same file or shared resource without worktree isolation and serial integration | serialize the units or isolate them in worktrees, then run actual file collision checks before integration |
| `subagent-forbidden-action-gap` | a subagent is allowed to stage, commit, push, mutate providers, control devices, run full suites, submit builds, post publicly, or make founder-only decisions | revise prompts, keep those actions orchestrator-owned, and record forbidden actions in `ORCHESTRATION.md` and `PROJECT_STATE.yaml` |
| `subagent-findings-unintegrated` | specialists return findings but state, failure cards, readiness docs, or validators are not updated | accept/reject each finding, update source docs and failure cards, then run focused validators plus the full suite |
| `apple-signing-simulator-only` | simulator build is treated as release readiness | prove Team ID, signing, bundle ID/App ID, app record, certificate/profile, archive/export/upload/TestFlight |
| `apple-pre-upload-preflight-skipped` | build archived and uploaded without running the pre-archive/export/upload preflight checklist — SDK keys not verified in Info.plist, `plutil -lint` not run on `PrivacyInfo.xcprivacy`, `NSPrivacyAccessedAPITypes` not scanned against UserDefaults/required-reason usage, exportArchive session-auth flags absent, or screenshot capture dimensions not verified before upload | run the preflight checklist in `apple-signing-release.md` section "Pre-Archive/Export/Upload Preflight Checklist", record all five sign-off lines in `APPLE_SIGNING.md`, re-archive and re-upload only after all items pass, then run `npm run check:apple-signing -- --root .` |
| `aso-metadata-appkittie-unvalidated` | keyword field is locked without AppKittie batch_keyword_difficulty / get_keyword_difficulty evidence, description field is < 2500 chars with no brevity rationale, or APP_STORE_LISTING.md artifact is missing after a metadata work session | invoke AppKittie on the full keyword set, record difficulty/volume scores and selection rationale in `APP_STORE_LISTING.md`, expand description to ≥ 2500 chars with keyword-rich body copy and conversion paragraphs (or record a founder-approved brevity note), create `APP_STORE_LISTING.md`, then run `npm run check:aso-metadata -- --root .` |
| `aso-metadata-stale-log-source` | live ASC metadata audit reads a `build/*.log` file instead of running `asc metadata pull` while the ASC CLI is available | run `asc metadata pull` (or the asc-metadata-sync read route), record the output path and diff timestamp in `APP_STORE_LISTING.md` or `STORE_CONSOLE.md`, then re-run the audit from live data |
| `google-play-data-safety-untraced` | Data safety answers do not trace to actual SDK/data behavior | map SDKs, data types, purposes, sharing, deletion, security, and Play Console fields |
| `cloudflare-routing-unverified` | domain/email routing exists without DNS/HTTP/email proof | verify HTTPS, DNS, security headers, routes, MX/TXT, and external email delivery |
| `landing-funnel-deploy-gates` | landing page deployed with uncommitted git changes; browser-rendered form smoke test not done; Alpine CSP/x-model incompatibility not pre-checked; wrangler major version not current; Cloudflare API token scope not verified before first deploy | satisfy all five pre-deploy gates (git clean, wrangler version, `wrangler whoami`, fresh build, deploy confirmed); run browser-rendered form smoke test to visible success state; audit Alpine event bindings for CSP compatibility before first deploy; then run `npm run check:landing-funnel -- --root .` |
| `paid-tool-routing-bypassed` | any primary paid tool (AppKittie, XPOZ, Higgsfield, Refero, MobAI) was skipped or replaced with a free fallback without: (a) a ToolSearch check confirming the MCP path is absent, (b) a founder confirmation prompt, and (c) a TOOL_DECISIONS.md entry with access status, founder confirmation, selected route, and fallback limitation | run ToolSearch for `mcp__<TOOLNAME>__*` to confirm absence, ask the founder, record the decision in TOOL_DECISIONS.md with all required fields, label any fallback outputs with a confidence note, then run `npm run check:paid-tool-decisions -- --root .` |
| `mobai-fallback-unapproved` | MobAI missing and agent uses XcodeBuildMCP without approval | record paid-tool decision and Apple-only fallback limitations before continuing |
| `mobai-bridge-dropped` | a build_run_sim or simulator relaunch invalidated the device registry and the agent retried stale UUIDs or skipped list_devices | call list_devices after every build/relaunch; call start_bridge if device absent; never reuse a pre-build UUID |
| `mobai-onboarding-chain-unverified` | 6+ onboarding steps were chained in a single DSL block without per-step screen verification, causing navigation to get stuck on the wrong screen | follow the Onboarding-Flow Navigation Pattern in mobai-toolbelt.md: verify screen label before each step, do not pre-choreograph without a clean dry-run |
| `design-canonical-conflict` | `design.md` or `DESIGN.md` labels a Higgsfield or other generated asset as "canonical visual target" while a locked native production component (SwiftUI, Flutter, React Native, etc.) is the founder's true canonical source, OR `CONTENT_ASSETS.md` manifest marks a generated asset `production`/`approved` when the native implementation is locked | correct all "canonical visual target" labels to "concept" or "direction" in `design.md`; add a `visual_lock` block to `DESIGN.md`; update manifest `status` fields to `concept` or `direction`; keep the locked native component as the canonical source (guarded by the `design-canonical-conflict` LaunchBench scenario) |
| `design-proof-missing` | visual work exists without rendered HTML using tokens | render `design.html`, screenshot proofs, and update `PROJECT_STATE.yaml` evidence |
| `review-prompt-teardown-race` | StoreKit/Play In-App Review request is triggered on an acceptance tap or any navigation action that dismisses the current screen, creating a view-teardown race | move the trigger to fire automatically after the value-reveal screen mounts (1-2s async delay), not bound to any tap that causes the screen to navigate away; verify with a build run using MobAI or XcodeBuildMCP |
| `analytics-event-invented-without-catalog` | new onboarding, coach, or tutorial event names are proposed or implemented without first cross-referencing `ANALYTICS.md` | add the event to `ANALYTICS.md` explicitly before referencing it in implementation code or docs; verify no new snake_case event names appear in code that are absent from `ANALYTICS.md` |
| `copy-ip-sweep-altered-owned-brand-vocabulary` | a copy rewrite targeting third-party IP risk also replaced or removed phrases owned by the product's brand voice (phrases in `DESIGN.md` §7 calibration set or `BRAND.md` owned-words list) | revert the brand-vocabulary changes; scope IP scanning to third-party names only; require founder approval for any changes to owned brand phrases |
| `landing-copy-compliance-failed` | landing copy contains ranked-cohort claims, unshipped-feature promises, unverifiable authority/clinical endorsements, lifetime-access promises not in `REVENUE_OPS.md`, or scarcity claims without live enforcement — shipped or near-shipped without a pre-edit scan | load `references/geo-seo.md` section 4 before the next edit, remove or evidence-back each claim, run `npm run check:landing-funnel -- --root .`, and block deploy until clean |
| `legal-contact-email-mx-unverified` | privacy/legal/support email addresses were written into public pages or legal documents before MX records existed for that domain, resulting in non-operational inboxes at deploy time | run `dig MX yourdomain.com +short` before writing contact addresses; if no MX records, use a tested fallback; update pages after MX is live; see `references/geo-seo.md` section 4a and `references/privacy-terms.md` section 5 |
| `asc-review-information-missing` | a build is submitted for App Review without the App Store Connect App Review Information Notes field populated, OR a no-login app is submitted without an explicit "no account / no demo needed" confirmation, causing a Guideline 2.1 "Information Needed" rejection and a lost review cycle | populate the App Review Information Notes with purpose+audience, setup/access instructions, demo credentials (or an explicit no-login confirmation that also clears the demo-account-required flag), test devices+OS, external services, regional differences, and regulated-material proof; record the notes in `APP_STORE_LISTING.md`; set them via the ASC review-details route (refresh `asc <cmd> --help`); run `npm run check:store-console -- --root .` |
| `asc-promoted-iap-image-duplicate` | a promoted in-app purchase or subscription uses a promotional image that is identical to the app icon, or the same image is reused across multiple promoted products, causing a Guideline 2.3.2 "Accurate Metadata" rejection | give each promoted IAP/subscription a unique 1024×1024 promotional image that depicts that specific product (not the app icon and not a duplicate of another product's image); generate distinct on-brand images (Higgsfield, tied to `DESIGN.md`), replace each via the ASC IAP/subscription image route, or delete the promotional image if the product will not be promoted; re-verify in App Store Connect before resubmission |
| `asc-flag-drift` | an agent uses an ASC CLI flag or subcommand form not confirmed by `asc <cmd> --help` or the Verified Command Cookbook (e.g. `asc validate app-store-version`, `--version` instead of `--version-id`), or retries a failed mutating command with a guessed flag instead of consulting `--help` | stop; run `asc <cmd> --help`; use the confirmed flag form (or the cookbook in `app-store-connect-cli.md`); never retry a mutating command more than once without confirmed flags; record the confirmed command in `STORE_CONSOLE.md`. Distinct from `asc-command-guidance-stale`, which covers upstream skill-pack changes |
| `revenuecat-missing-metadata-unresolved` | an App Store subscription product exists but stays in MISSING_METADATA, so RevenueCat returns an empty offering while a build is marked paywall-ready | create a subscription-group localization (display name + description) for each product, wait for Apple status to clear MISSING_METADATA, resubmit for review with founder approval, and block RevenueCat catalog mapping and paywall-ready claims until status is cleared |
| `revenuecat-product-type-mismatch` | a lifetime/one-time unlock is mapped in RevenueCat as `non_renewing_subscription` instead of `non_consumable` (it may silently expire), or an RC product type does not match its App Store counterpart | recreate the RC product with the correct type (App Store non-consumable → RC `non_consumable`; auto-renewable → RC `auto_renewable_subscription`), re-attach to the entitlement and offering, and prove a sandbox purchase grants the expected access |
| `revenuecat-debug-preview-masking` | a debug build seeds paywall packages when RevenueCat returns empty, hiding a broken production paywall (zero packages / "Purchases unavailable") from developer testing | audit the Release scheme for any code path that populates packages when the RC fetch returns nil/empty; run a Release-build paywall smoke check (MobAI or XcodeBuildMCP against sandbox) confirming `currentOffering?.packages` is non-empty before any TestFlight upload is marked paywall-ready |
| `doppler-auth-missing` | Doppler is the selected secret provider but the shell is not authenticated (`doppler me` non-zero / `DOPPLER_TOKEN_MISSING`), so secret-bearing commands silently skip injection for an entire session | run `doppler login` (founder/operator action), set up a service token for CI/non-interactive use, re-run `check:secrets`, and record auth proof in `SECRETS.md`; block secret-bearing work until `doppler me` exits 0 |
| `wrangler-toml-credentials-committed` | `wrangler.toml`/`wrangler.json` `[vars]` (or another committed config) contains a real credential value (Supabase URL/key, API key) instead of a routed secret | move the value to Cloudflare secrets (`wrangler secret put`) or Doppler-injected env vars, remove it from `[vars]`, audit git history for exposure, then run `npm run check:secrets -- --root .` |
| `project-state-stale-after-upload` | a build is uploaded, a store-ops event occurs (custom product page, in-app event, iPad target, TestFlight group, app record), or an audit produces evidence, but `PROJECT_STATE.yaml` (`updated_at`, lane status/evidence, build fields), `PRODUCTION_READINESS.md`, and `launch-cockpit.html` are not updated to match | update `PROJECT_STATE.yaml` `updated_at` and the affected lane status/evidence/build fields, write any stated `PRODUCTION_READINESS.md` evidence to the file, re-render `launch-cockpit.html`, then commit; `npm run validate:launch-state -- --root .` is a partial structural check only and does not detect session-relative staleness |
| `test-suite-green-contracts-unproven` | a unit/UI test suite shows all-pass and is cited as proof that paywall, RevenueCat offerings, or attribution work while those provider contracts are acknowledged broken or untested | pair the green suite with backend/provider proof: RevenueCat returns ≥1 package and entitlement grants in app + dashboard, `self_reported_source` appears in PostHog person properties; attach the proof to `PRODUCTION_READINESS.md` before any paywall/attribution-ready claim |
| `asc-auth-not-set-up` | an agent reports it "cannot access App Store Connect" (or marks an ASC lane blocked) when the `asc` CLI is installed and read access is healthy, but auth/app-record/cert is simply not set up yet — e.g. it gave up after `source <profile>.env` threw a shell error, or never checked for an existing keychain profile | run the ASC Auth Setup And Recovery ladder in `app-store-connect-cli.md`: `asc auth doctor`; try an existing keychain profile (`asc --profile <Name>`, account-level keys reach all apps); or set `ASC_KEY_ID`/`ASC_ISSUER_ID`/`ASC_PRIVATE_KEY_PATH` / `asc auth init`/`login`; treat a missing app record/cert as a founder-gated setup step with the exact next command, never as "cannot access ASC" |
| `change-cascade-incomplete` | an app feature, copy, brand-term, pricing, or product change shipped without checking the surfaces it impacts (App Store listing/keywords/screenshots/captions, App Preview, in-app events, custom product pages, IAP/subscription display names + promo images, all localizations, landing page + meta/JSON-LD, SEO/GEO, lifecycle email, ASC review notes, RevenueCat product names, analytics event names) | run the Change Cascade Map in `references/change-cascade.md`: enumerate the impacted surfaces for the change type, update each (or record why it is unaffected), re-render screenshots/listings where copy or UI changed, and reconcile the lexicon across surfaces before calling the change done |

## Closing A Card

A card can move to `resolved` only when:

- the fix is implemented or the lane is explicitly deferred/not needed
- evidence path or command proof is attached
- downstream docs and `PROJECT_STATE.yaml` are updated
- any founder approval is recorded

Do not close a card by saying the issue is "noted."
